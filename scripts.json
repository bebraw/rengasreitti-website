{
  "scripts": {
    "install:gustwind": "deno install -A --unstable --no-check -f https://deno.land/x/gustwind@v0.20.2/cli.ts",
    "build:site": "gustwind -b",
    "start": "gustwind -d",
    "ci:build": "deno run -A --unstable --no-check https://deno.land/x/gustwind@v0.20.2/cli.ts -b"
  }
}
